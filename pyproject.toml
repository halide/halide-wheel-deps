[project]
name = "halide-wheel-deps"
version = "0.1.0"
description = "Python wheels for C++ dependencies of Halide (flatbuffers, wabt)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.uv.workspace]
members = ["packages/flatbuffers", "packages/wabt"]
